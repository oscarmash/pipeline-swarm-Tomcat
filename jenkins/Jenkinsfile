pipeline {
    agent any

    stages {
        stage('Prepare') {
            steps {
		echo 'Verificar git..'
		checkout scm
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
		echo "BUILD NUMBER: ${env.BUILD_NUMBER}"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
